package com.bankManagement.Sources;

/**
 * @Description: this is an enum that defines transaction types for easier
 * way to manage them.
 */

public enum TransactionTypes {
    deposit, withdrawal, transfer_in, transfer_out
}
