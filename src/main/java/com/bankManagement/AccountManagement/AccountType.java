package com.bankManagement.AccountManagement;

/**
 * @Description This is an enum that defines account type to make easier
 * log in and registration (take a look there to understand)
 */

public enum AccountType {
    user, employee
}
